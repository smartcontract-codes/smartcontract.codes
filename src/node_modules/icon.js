const bel = require('bel')

module.exports = icon

function icon(name, svg) {
    return bel`
        <svg viewBox="0 0 54 54">
            <g class=${name}>
                ${svg.map(d => {
                    return bel`<path d="${d}" />`
                })}
            </g>
        </svg>`
}